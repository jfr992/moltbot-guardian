# OpenClaw Sentinel Configuration

# Port for the dashboard
PORT=5056

# OpenClaw Gateway WebSocket URL
# For Docker: use host.docker.internal to reach host machine
OPENCLAW_GATEWAY_URL=ws://host.docker.internal:18789

# OpenClaw Gateway auth token (required if gateway has auth enabled)
# Get it from: jq -r '.gateway.auth.token' ~/.openclaw/openclaw.json
OPENCLAW_GATEWAY_TOKEN=

# OpenClaw data directory (for reading session logs)
# Default: ~/.openclaw
OPENCLAW_DIR=~/.openclaw

# Gemini API key (for memory status - optional)
GEMINI_API_KEY=
